# File: main.py
import app_gui  # Import the main application GUI class

if __name__ == "__main__":
    # Create the main application window instance
    app = app_gui.Application()
    # Start the Tkinter event loop
    app.mainloop()
